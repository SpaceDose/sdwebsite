<div class="container">

        <h1>Your profile (<%= current_user.username %>):</h1>
        <br><br>

        <div class="row">
          <div class="col">
            <h3>Nickname</h3>
          </div>
          <div class="col-4">
            <h3>Ts Identity</h3>
          </div>
          <div class="col">
            <h3>Points</h3>
          </div>
          <div class="col">

          </div>
        </div>

        <!-- Spacer -->
        <div class="dropdown-divider"></div>

        <div class="row">
          <div class="col">
            <p><%= current_user.tsusername %></p>
          </div>
          <div class="col-4">
            <p><%= current_user.uniqueid %></p>
          </div>
          <div class="col align-self-end">
            <p>
              <% @usr = Point.find_by uniqueid: current_user.uniqueid %>
              <% if @usr %>
                <%= @usr.points %>
              <% else %>
                WRONG IDENTITY
              <% end %>
            </p>
          </div>
          <div class="col">
            <%= link_to "Edit", edit_profile_path, class: "btn btn-outline-warning" %>
          </div>
        </div>

        <div style="margin-top: 100px">
          <h1>Please link your Unique ID in your profile</h1>
          <br><br>
          <h2>How to find your TeamSpeak Identity?</h2>
          <br>
          <p>To see your points, its required that you add your TeamSpeak Unique ID to your profile.</p>
          <p>You can find your Ts³ Identity (Unique ID) by pressing CTRL+I in your Ts³ Client and clicking on "Go advanced"</p>
        </div>

</div>
