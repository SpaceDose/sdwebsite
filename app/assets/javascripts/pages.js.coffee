# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

#$(document).on 'turbolinks:load', ->
#$(window).load ->

#$(window).ready ->
#  #return unless $('meta[name=psj]').attr('controller') == 'pages' && $('meta[name=psj]').attr('action') == 'users'
#  count = 0
#  while count < gon.users_count
#    document.getElementById('username_' + count).innerHTML = 'test' + count
#    document.getElementById('points_' + count).innerHTML = 'test'
#    count++
#  alert 'All images are loaded'
  
